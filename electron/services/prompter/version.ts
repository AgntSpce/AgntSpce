// agntspce-prompter version
// Ported from LLMLingua llmlingua/version.py
// Renamed and integrated as internal agntspce-prompter module.
// This module is internal-only and must not be published standalone.

const MAJOR = '0'
const MINOR = '1'
const PATCH = '0'
const SUFFIX = ''

export const VERSION_SHORT = `${MAJOR}.${MINOR}`
export const VERSION = `${MAJOR}.${MINOR}.${PATCH}${SUFFIX}`
export const AGNTSPCE_PROMPTER_VERSION = VERSION
